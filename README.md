# author_manager_python

## install pacakges used

``pip install -r requirements. txt`

## start server

`python3 ./src/run.py `
or affter setting the ` FLASK_APP`

`flask run`

## you can run test using the nose package install

``nose2 api.tests`
